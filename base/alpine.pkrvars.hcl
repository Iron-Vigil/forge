# Shared Alpine base version — referenced by all image builds.
# alpine-version-check.yml files an issue when a newer stable branch ships;
# Renovate opens the bump PR automatically once it's enabled org-wide.
alpine_version = "3.24"
