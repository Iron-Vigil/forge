# Populate with the nginx signing key from https://nginx.org/en/pgp_keys.html
# Import with: gpg --import pgp-key.asc
# Verify fingerprints against the official page before committing.
# Leave this file empty (just comments) to skip PGP verification — SHA256 still runs.
